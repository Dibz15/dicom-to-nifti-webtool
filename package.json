{
  "name": "dicom-to-nifti",
  "private": true,
  "version": "1.0.0",
  "description": "Self-contained, offline-capable DICOM to NIfTI converter (dcm2niix WASM), built as a single static HTML file for GitHub Pages.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "npx --yes serve dist"
  },
  "dependencies": {
    "@niivue/dcm2niix": "^1.2.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
